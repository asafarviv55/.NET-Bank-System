@page
@model Bank.Pages.PassBackOperations.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>PassBackOperation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="PassBackOperation.is_charge" /> @Html.DisplayNameFor(model => model.PassBackOperation.is_charge)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="PassBackOperation.account_balance" class="control-label"></label>
                <input asp-for="PassBackOperation.account_balance" class="form-control" />
                <span asp-validation-for="PassBackOperation.account_balance" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PassBackOperation.created_at" class="control-label"></label>
                <input asp-for="PassBackOperation.created_at" class="form-control" />
                <span asp-validation-for="PassBackOperation.created_at" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
