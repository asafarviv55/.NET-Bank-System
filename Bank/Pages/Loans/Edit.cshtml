@page
@model Bank.Pages.Loans.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Loan</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Loan.id" />
            <div class="form-group">
                <label asp-for="Loan.loan_balance" class="control-label"></label>
                <input asp-for="Loan.loan_balance" class="form-control" />
                <span asp-validation-for="Loan.loan_balance" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Loan.created_at" class="control-label"></label>
                <input asp-for="Loan.created_at" class="form-control" />
                <span asp-validation-for="Loan.created_at" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
